// MUI Imports
import { createTheme } from "@mui/material";

const ColorTheme = createTheme({
    palette: {
        primary: {
            main: '#ffee58',
        },
        secondary: {
            main: '#00bcd4',
        },
    },
})

export default ColorTheme;